const config = {
  BASE_URL: "https://backend-api.performacemedia.com:8000", // Change this when deploying
  // BASE_URL: "http://localhost:8000", // Change this when deploying
};

export default config;
